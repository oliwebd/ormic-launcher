{
  "name": "ormic-launcher",
  "version": "0.1.9",
  "description": "Ormic Launcher GNOME Extension in TypeScript",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/generate-accent-css.mjs && prettier --write 'dist/**/*.js' && node scripts/format-dist.mjs",
    "local-install": "make install",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@girs/gjs": "4.0.4",
    "@girs/gnome-shell": "^50.0.0",
    "eslint": "^10.4.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.0"
  }
}